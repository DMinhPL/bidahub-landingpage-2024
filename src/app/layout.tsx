import Footer from "@/components/Footer";
import Header from "@/components/Header";
import { mySvnDays, svnPoppins } from "@/utils/fonts";
import type { Metadata } from "next";
import Image from "next/image";
import bgBody from '../assets/images/background.png';
import "./globals.css";
export const metadata: Metadata = {
  title: "Bidahub",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${svnPoppins.className} ${mySvnDays.variable} antialiased relative`}
      >
        <Image src={bgBody.src} width={100} height={100} className="w-full h-full object-cover absolute top-0 left-0 z-[1]" alt="bg" />
        <Header />
        <main className="pt-[140px] lg:pt-[175px] relative z-[2]">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
